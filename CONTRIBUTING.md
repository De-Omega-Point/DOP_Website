# Contributing

## Development

```bash
npm ci
npm run dev
```

## Verification

Before opening a pull request:

```bash
npm run build
```

## Pull requests

Keep changes focused, explain the user value, and include screenshots for visible interface changes. Do not commit generated `dist/` files, credentials, private keys, `.env` files, or downloaded model caches.
